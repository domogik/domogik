# /etc/cron.d/domogik
#  This cron item will help domogik to do some periodic tasks
#  All these tasks are handled by dmg_cron tool

13 *     * * *     root   [ -x /usr/local/bin/dmg_cron ] && /usr/local/bin/dmg_cron
